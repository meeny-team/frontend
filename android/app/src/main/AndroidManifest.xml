<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>

        <!-- 커스텀 스킴 fallback — 다른 앱 (KakaoTalk 공유 등) 이 https 링크를 앱으로 -->
        <!-- 열지 못하는 케이스를 커버. 정상적으로는 아래 App Links (autoVerify) 를 사용. -->
        <intent-filter>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="meeny" android:host="invite" />
        </intent-filter>

        <!-- Android App Links (autoVerify=true) —
             /.well-known/assetlinks.json 을 도메인 루트에 배포하면 시스템이 자동 검증해
             https 링크가 브라우저를 거치지 않고 앱에서 바로 열린다. docs/deeplinks.md 참고. -->
        <intent-filter android:autoVerify="true">
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" android:host="meeny.store" android:pathPrefix="/invite/" />
            <data android:scheme="https" android:host="www.meeny.store" android:pathPrefix="/invite/" />
        </intent-filter>
      </activity>

      <!-- 카카오 SDK가 카카오톡 미설치 단말에서 카카오계정 웹 로그인 후 앱으로 돌아올 때 사용하는 deep link.
           scheme은 "kakao" + {네이티브 앱 키}. 카카오 콘솔 등록 패키지명/키와 정확히 일치해야 함. -->
      <activity
        android:name="com.kakao.sdk.auth.AuthCodeHandlerActivity"
        android:exported="true">
        <intent-filter>
          <action android:name="android.intent.action.VIEW" />
          <category android:name="android.intent.category.DEFAULT" />
          <category android:name="android.intent.category.BROWSABLE" />
          <data android:host="oauth" android:scheme="kakao4d40392ebab5c4c6c28a02ea8461ec2b" />
        </intent-filter>
      </activity>
    </application>
</manifest>
